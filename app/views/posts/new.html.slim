= form_for @post do |form|
  = form.label :title
  = form.text_field :title
  = form.label :body
  = form.text_area :body
  = form.label :image
  = form.file_field :image
  = form.submit "Criar post"
